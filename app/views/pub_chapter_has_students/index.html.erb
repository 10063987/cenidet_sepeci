<p id="notice"><%= notice %></p>

<h1>Pub Chapter Has Students</h1>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th>Pub chapter</th>
      <th>Student</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pub_chapter_has_students.each do |pub_chapter_has_student| %>
      <tr>
        <td><%= pub_chapter_has_student.status %></td>
        <td><%= pub_chapter_has_student.pub_chapter %></td>
        <td><%= pub_chapter_has_student.student %></td>
        <td><%= link_to 'Show', pub_chapter_has_student %></td>
        <td><%= link_to 'Edit', edit_pub_chapter_has_student_path(pub_chapter_has_student) %></td>
        <td><%= link_to 'Destroy', pub_chapter_has_student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pub Chapter Has Student', new_pub_chapter_has_student_path %>
