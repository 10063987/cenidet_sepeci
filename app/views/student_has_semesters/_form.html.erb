<%= form_with(model: student_has_semester, local: true) do |form| %>
  <% if student_has_semester.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_has_semester.errors.count, "error") %> prohibited this student_has_semester from being saved:</h2>

      <ul>
      <% student_has_semester.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :concluido %>
    <%= form.text_field :concluido, id: :student_has_semester_concluido %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :student_has_semester_student_id %>
  </div>

  <div class="field">
    <%= form.label :semester_id %>
    <%= form.text_field :semester_id, id: :student_has_semester_semester_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
