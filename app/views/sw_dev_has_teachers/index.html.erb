<p id="notice"><%= notice %></p>

<h1>Sw Dev Has Teachers</h1>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th>Sw dev</th>
      <th>Teacher</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sw_dev_has_teachers.each do |sw_dev_has_teacher| %>
      <tr>
        <td><%= sw_dev_has_teacher.status %></td>
        <td><%= sw_dev_has_teacher.sw_dev %></td>
        <td><%= sw_dev_has_teacher.teacher %></td>
        <td><%= link_to 'Show', sw_dev_has_teacher %></td>
        <td><%= link_to 'Edit', edit_sw_dev_has_teacher_path(sw_dev_has_teacher) %></td>
        <td><%= link_to 'Destroy', sw_dev_has_teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sw Dev Has Teacher', new_sw_dev_has_teacher_path %>
