<%= form_with(model: sw_dev_has_teacher, local: true) do |form| %>
  <% if sw_dev_has_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sw_dev_has_teacher.errors.count, "error") %> prohibited this sw_dev_has_teacher from being saved:</h2>

      <ul>
      <% sw_dev_has_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :sw_dev_has_teacher_status %>
  </div>

  <div class="field">
    <%= form.label :sw_dev_id %>
    <%= form.text_field :sw_dev_id, id: :sw_dev_has_teacher_sw_dev_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id, id: :sw_dev_has_teacher_teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
