<p id="notice"><%= notice %></p>

<h1>Pub Chapter Has Teachers</h1>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th>Pub chapter</th>
      <th>Teacher</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pub_chapter_has_teachers.each do |pub_chapter_has_teacher| %>
      <tr>
        <td><%= pub_chapter_has_teacher.status %></td>
        <td><%= pub_chapter_has_teacher.pub_chapter %></td>
        <td><%= pub_chapter_has_teacher.teacher %></td>
        <td><%= link_to 'Show', pub_chapter_has_teacher %></td>
        <td><%= link_to 'Edit', edit_pub_chapter_has_teacher_path(pub_chapter_has_teacher) %></td>
        <td><%= link_to 'Destroy', pub_chapter_has_teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pub Chapter Has Teacher', new_pub_chapter_has_teacher_path %>
