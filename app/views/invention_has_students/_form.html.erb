<%= form_with(model: invention_has_student, local: true) do |form| %>
  <% if invention_has_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invention_has_student.errors.count, "error") %> prohibited this invention_has_student from being saved:</h2>

      <ul>
      <% invention_has_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :invention_has_student_status %>
  </div>

  <div class="field">
    <%= form.label :invention_id %>
    <%= form.text_field :invention_id, id: :invention_has_student_invention_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :invention_has_student_student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
