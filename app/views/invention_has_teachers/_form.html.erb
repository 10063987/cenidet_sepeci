<%= form_with(model: invention_has_teacher, local: true) do |form| %>
  <% if invention_has_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invention_has_teacher.errors.count, "error") %> prohibited this invention_has_teacher from being saved:</h2>

      <ul>
      <% invention_has_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :invention_has_teacher_status %>
  </div>

  <div class="field">
    <%= form.label :invention_id %>
    <%= form.text_field :invention_id, id: :invention_has_teacher_invention_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id, id: :invention_has_teacher_teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
