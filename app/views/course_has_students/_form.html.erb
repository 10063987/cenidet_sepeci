<%= form_with(model: course_has_student, local: true) do |form| %>
  <% if course_has_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_has_student.errors.count, "error") %> prohibited this course_has_student from being saved:</h2>

      <ul>
      <% course_has_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :course_has_student_status %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id, id: :course_has_student_course_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :course_has_student_student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
